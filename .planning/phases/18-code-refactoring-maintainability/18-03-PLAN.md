---
phase: 18-code-refactoring-maintainability
plan: 03
type: execute
wave: 3
depends_on:
  - 18-02
files_modified:
  - admin/app/users/page.tsx
  - admin/app/quotes/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "users/page.tsx uses new ListPage or use-list-data hook"
    - "quotes/page.tsx uses new ListPage or use-list-data hook"
    - "Code duplication reduced by 60%+"
  artifacts:
    - path: "admin/app/users/page.tsx"
      provides: "Refactored users page"
      max_lines: 150
    - path: "admin/app/quotes/page.tsx"
      provides: "Refactored quotes page"
      max_lines: 150
  key_links:
    - from: "admin/app/users/page.tsx"
      to: "admin/hooks/use-list-data.ts"
      via: "import"
    - from: "admin/app/quotes/page.tsx"
      to: "admin/lib/types/index.ts"
      via: "import"
---

<objective>
Refactor users and quotes pages to use reusable components.

Purpose: Eliminate duplicate code patterns, apply centralized types.
Output: Simplified pages that delegate to reusable hook/component.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@admin/app/users/page.tsx
@admin/app/quotes/page.tsx
@admin/lib/types/index.ts
@admin/hooks/use-list-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor users/page.tsx</name>
  <files>admin/app/users/page.tsx</files>
  <action>
Refactor admin/app/users/page.tsx:
1. Import User, UserStats from admin/lib/types/index.ts (replace inline interfaces)
2. Import and use use-list-data hook OR use ListPage component
3. Remove inline type definitions (lines 41-56) - use centralized types
4. Keep exact same functionality - checkboxes, dropdown menus, routing work
5. If using hook, move fetch logic into hook's fetchFn

This is REFACTOR ONLY - no feature changes. Verify all functionality works after.
  </action>
  <verify>
<automated>tsc --noEmit admin/app/users/page.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
users/page.tsx compiles without errors, imports types from centralized location
</done>
</task>

<task type="auto">
  <name>Task 2: Refactor quotes/page.tsx</name>
<files>admin/app/quotes/page.tsx</files>
  <action>
Refactor admin/app/quotes/page.tsx:
1. Import Quote, QuoteStats, QuoteStatus from admin/lib/types/index.ts
2. Import and use use-list-data hook OR use ListPage component
3. Remove inline type definitions (lines 39-57) - use centralized types
4. Remove duplicate variants object - will be handled by centralized type or component
5. Keep exact same functionality - checkboxes, dropdown menus work

This mirrors users/page.tsx refactor to ensure consistency.
  </action>
  <verify>
<automated>tsc --noEmit admin/app/quotes/page.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
quotes/page.tsx compiles without errors, imports types from centralized location
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page→api→backend | data flow only, no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Tampering | refactored pages | accept | Types already validated upstream |
| T-18-09 | Information Disclosure | refactored pages | accept | No PII added by refactor |

No new security concerns - refactoring preserves existing behavior.
</threat_model>

<verification>
1. Run TypeScript check on both pages: tsc --noEmit
2. Verify no inline types: grep "interface User" admin/app/users/page.tsx
3. Verify no inline types: grep "interface Quote" admin/app/quotes/page.tsx
4. Verify imports: grep "import.*types" admin/app/users/page.tsx
</verification>

<success_criteria>
- [ ] users/page.tsx uses centralized types
- [ ] quotes/page.tsx uses centralized types
- [ ] Both compile without TypeScript errors
- [ ] No functionality broken - verify UI renders correctly
</success_criteria>

<output>
After completion, create `.planning/phases/18-code-refactoring-maintainability/18-03-SUMMARY.md`
</output>