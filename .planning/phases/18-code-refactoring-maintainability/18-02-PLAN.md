---
phase: 18-code-refactoring-maintainability
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - admin/hooks/use-list-data.ts
  - admin/components/list-page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Reusable hook handles pagination, search, filter"
    - "ListPage component accepts configurable props"
    - "users/page.tsx and quotes/page.tsx can use the new components"
  artifacts:
    - path: "admin/hooks/use-list-data.ts"
      provides: "Reusable data fetching hook"
      min_lines: 80
    - path: "admin/components/list-page.tsx"
      provides: "Reusable list page template"
      min_lines: 100
  key_links:
    - from: "admin/hooks/use-list-data.ts"
      to: "admin/lib/types/index.ts"
      via: "import"
      pattern: "import.*types"
---

<objective>
Create reusable data fetching hook and list page component.

Purpose: Eliminates duplicate code between users/page.tsx and quotes/page.tsx.
Output: use-list-data.ts hook and list-page.tsx component.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@admin/lib/types/index.ts
@admin/app/users/page.tsx
@admin/app/quotes/page.tsx
@admin/components/search-tabs.tsx
@admin/components/ui/pagination.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create use-list-data hook</name>
  <files>admin/hooks/use-list-data.ts</files>
  <action>
Create reusable data fetching hook at admin/hooks/use-list-data.ts:
1. Accept fetchFn: (params: URLSearchParams) => Promise<PaginatedResponse<T>>
2. Accept onDataChange?: (data: T[]) => void for side effects
3. Manage state: data, loading, error, pagination (page, limit, total, totalPages)
4. Manage filters: search, filterKey, filterValue
5. Debounce search input (300ms)
6. Expose refresh() to manually reload
7. Return { data, loading, error, pagination, filters, refresh, setPage, setLimit, setSearch, setFilter }

This is used by pages that fetch paginated data - matches existing patterns from users and quotes.
  </action>
  <verify>
<automated>tsc --noEmit admin/hooks/use-list-data.ts 2>&1 | head -20</automated>
  </verify>
  <done>
Hook file exists with >80 lines, handles pagination/search/filter state
</done>
</task>

<task type="auto">
  <name>Task 2: Create list-page component</name>
  <files>admin/components/list-page.tsx</files>
  <action>
Create reusable list page component at admin/components/list-page.tsx:
1. Props: title, description, fetchData, columns, filters, statsComponent (optional)
2. Use use-list-data hook internally
3. Render SearchTabs with provided filters
4. Render table with provided columns
5. Render pagination with hook's pagination state
6. Render optional stats cards above table

Accept generic type T for the data items.
  </action>
  <verify>
<automated>tsc --noEmit admin/components/list-page.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
Component exists with >100 lines, accepts columns/filters as props
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component→hook→api | data flow only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Tampering | use-list-data | accept | Uses existing api.ts client - integrity already handled |
| T-18-07 | Denial of Service | use-list-data | accept | Debounce prevents request spam |

No new security concerns - this is UI layer refactoring.
</threat_model>

<verification>
1. Run TypeScript check: tsc --noEmit on the new files
2. Verify hook exports: grep "export.*useListData" admin/hooks/use-list-data.ts
3. Verify component exists: ls admin/components/list-page.tsx
</verification>

<success_criteria>
- [ ] admin/hooks/use-list-data.ts created with reusable pagination/search/filter logic
- [ ] admin/components/list-page.tsx created as reusable template
- [ ] Both files compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-code-refactoring-maintainability/18-02-SUMMARY.md`
</output>