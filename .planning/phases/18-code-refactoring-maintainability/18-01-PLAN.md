---
phase: 18-code-refactoring-maintainability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - admin/lib/types/index.ts
  - admin/lib/api.ts
  - backend/src/modules/shipments/shipments.service.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Types extracted to central location"
    - "api.ts has TypeScript generics and comments"
    - "Shipments service has separated concerns"
  artifacts:
    - path: "admin/lib/types/index.ts"
      provides: "Centralized type definitions"
      min_lines: 50
    - path: "admin/lib/api.ts"
      provides: "Typed API client with generics"
      exports: ["api", "ApiError", "ApiResponse"]
    - path: "backend/src/modules/shipments/shipments.utils.ts"
      provides: "White label code generation"
      min_lines: 30
  key_links:
    - from: "admin/lib/api.ts"
      to: "admin/lib/types/index.ts"
      via: "import"
      pattern: "import.*types"
---

<objective>
Extract centralized types and refactor core infrastructure files (api.ts, shipments service).

Purpose: This establishes the foundation for all subsequent refactoring tasks.
Output: Typed types, improved api.ts, and separated backend utilities.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@admin/lib/api.ts
@admin/app/users/page.tsx
@admin/app/quotes/page.tsx
@backend/src/modules/shipments/shipments.service.ts

# Existing type definitions to extract:
# - User interface (users/page.tsx lines 41-49)
# - Stats interface (users/page.tsx lines 51-56)
# - Quote interface (quotes/page.tsx lines 39-49)
# - QuoteStatus type (quotes/page.tsx line 39)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract centralized types</name>
  <files>admin/lib/types/index.ts</files>
  <action>
Extract all inline types from users/page.tsx and quotes/page.tsx into centralized admin/lib/types/index.ts:
1. Create User interface with id, name, email, role, isActive, organisationId, createdAt
2. Create UserStats interface with total, active, customers, staff
3. Create Quote interface with id, email, originCountry, destinationCountry, status, price, createdAt
4. Create QuoteStatus type as union: "pending" | "quoted" | "accepted" | "rejected"
5. Create QuoteStats interface with total, pending, quoted, accepted, rejected
6. Create PaginatedResponse<T> generic with data, total, page, limit, totalPages

Add JSDoc comments explaining each type.
  </action>
  <verify>
<automated>tsc --noEmit admin/lib/types/index.ts 2>&1 | head -20</automated>
  </verify>
  <done>
Types file exists at admin/lib/types/index.ts with >50 lines, exports at least 6 types
</done>
</task>

<task type="auto">
  <name>Task 2: Refactor api.ts with generics and comments</name>
  <files>admin/lib/api.ts</files>
  <action>
Refactor admin/lib/api.ts:
1. Import types from admin/lib/types/index.ts
2. Add generic typed response methods: get<T>, post<T>, patch<T>, put<T>, delete<T>
3. Add AbortController export so callers can cancel requests
4. Add JSDoc comments to public methods explaining parameters and return values
5. Add request timeout configuration option (currently hardcoded to 5000ms)

Keep existing functionality - this is refactor only, not feature change.
  </action>
  <verify>
<automated>tsc --noEmit admin/lib/api.ts 2>&1 | head -20</automated>
  </verify>
  <done>
api.ts has TypeScript generics, JSDoc comments on public methods, exports AbortController
</done>
</task>

<task type="auto">
  <name>Task 3: Extract shipments utilities</name>
  <files>backend/src/modules/shipments/shipments.utils.ts</files>
  <action>
Extract from shipments.service.ts into new shipments.utils.ts:
1. Move generateWhiteLabelCode() method to utils as generateWhiteLabelCode(): string
2. Add JSDoc explaining the 14-digit numeric code generation logic
3. Export the function

DO NOT change any logic - straight extraction with added comments.
  </action>
  <verify>
<automated>tsc --noEmit backend/src/modules/shipments/shipments.utils.ts 2>&1 | head -20</automated>
  </verify>
  <done>
shipments.utils.ts exists with generateWhiteLabelCode export, JSDoc on method
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→api | untrusted input crosses here via fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | api.ts | mitigate | Types ensure response shape validation - no changes needed |
| T-18-02 | Repudiation | api.ts | accept | Logging already sufficient |
| T-18-03 | Information Disclosure | api.ts | accept | No PII exposed in this refactor |
| T-18-04 | Denial of Service | api.ts | accept | Existing timeout sufficient |
| T-18-05 | Elevation of Privilege | api.ts | accept | Auth handled elsewhere |

No new threats introduced - pure refactoring.
</threat_model>

<verification>
1. Run TypeScript check: tsc --noEmit (should pass)
2. Check types file exports: grep "^export" admin/lib/types/index.ts
3. Check api.ts has generics: grep "async get<T>" admin/lib/api.ts
4. Check utils exports: grep "export.*generateWhiteLabelCode" backend/src/modules/shipments/shipments.utils.ts
</verification>

<success_criteria>
- [ ] admin/lib/types/index.ts exists with all 6+ type definitions
- [ ] admin/lib/api.ts uses imported types with generics
- [ ] backend/src/modules/shipments/shipments.utils.ts extracted
- [ ] No functionality changed - all existing behavior preserved
</success_criteria>

<output>
After completion, create `.planning/phases/18-code-refactoring-maintainability/18-01-SUMMARY.md`
</output>