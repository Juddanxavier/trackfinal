---
phase: 17-quote-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/quotes/quotes.controller.ts
  - backend/src/modules/quotes/quotes.service.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "API supports pagination, filtering, sorting for quotes list"
    - "API supports DELETE quote (admin only)"
    - "API provides stats for dashboard cards"
    - "Email triggers send when status changes to quoted/accepted/rejected"
  artifacts:
    - path: "backend/src/modules/quotes/quotes.controller.ts"
      provides: "API endpoints: GET /quotes, DELETE /quotes/:id, GET /quotes/stats"
      min_lines: 30
    - path: "backend/src/modules/quotes/quotes.service.ts"
      provides: "Service methods: delete, paginated find, stats, email triggers"
      min_lines: 30
  key_links:
    - from: "quotes.controller.ts"
      to: "quotes.service.ts"
      via: "service method calls"
      pattern: "this.quotesService\\.(delete|paginated|stats)"
---

<objective>
Backend Extension for Quote Management

Extend backend quotes module to support frontend requirements: DELETE endpoint, pagination, filtering, sorting, stats endpoint, and email triggers for status changes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-quote-management-we-need-same-layout-as-user-management-lets/17-CONTEXT.md
@.planning/phases/17-quote-management-we-need-same-layout-as-user-management-lets/17-RESEARCH.md
@backend/src/modules/quotes/quotes.controller.ts
@backend/src/modules/quotes/quotes.service.ts
@backend/src/database/schema/quotes.ts

**Key from RESEARCH.md:**
- Missing: DELETE, GET by ID, pagination, filtering, sorting, stats, email triggers
- EventsGateway helpers exist: emitToOrganisation, emitToUser
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DELETE endpoint and service method</name>
  <files>backend/src/modules/quotes/quotes.controller.ts,backend/src/modules/quotes/quotes.service.ts</files>
  <read_first>
- backend/src/modules/quotes/quotes.controller.ts (existing endpoints pattern)
- backend/src/modules/quotes/quotes.service.ts (existing service methods)
- backend/src/database/schema/quotes.ts (quote model)
  </read_first>
  <action>
Add DELETE /quotes/:id endpoint to quotes.controller.ts:
- Use @Roles(Role.ADMIN) guard (admin only per D-03)
- Call quotesService.delete(id) method

Add delete method to quotes.service.ts:
- Use db.delete(quotes).where(eq(quotes.id, id))
- Return success message
  </action>
  <verify>
grep -n "Delete.*:id" backend/src/modules/quotes/quotes.controller.ts
grep -n "async delete" backend/src/modules/quotes/quotes.service.ts
  </verify>
  <done>DELETE /quotes/:id returns 200 for admin, 403 for non-admin</done>
</task>

<task type="auto">
  <name>Task 2: Add pagination, filtering, sorting to GET /quotes</name>
  <files>backend/src/modules/quotes/quotes.controller.ts,backend/src/modules/quotes/quotes.service.ts</files>
  <read_first>
- backend/src/modules/quotes/quotes.controller.ts (existing GET /quotes)
- admin/app/users/page.tsx (filter/sort pattern to match)
  </read_first>
  <action>
Extend GET /quotes endpoint with query params:
- page?: number (default: 1)
- limit?: number (default: 10)
- status?: 'pending' | 'quoted' | 'accepted' | 'rejected'
- search?: string (search email, origin, destination)
- sortBy?: 'createdAt' | 'updatedAt' | 'email' | 'originCountry' | 'destinationCountry'
- sortOrder?: 'asc' | 'desc'

Add paginatedFind method to quotes.service.ts:
- Use db.select().from(quotes).where(conditions)
- Apply offset/limit for pagination
- Return { data, total, page, limit, totalPages }
  </action>
  <verify>
grep -n "page.*limit" backend/src/modules/quotes/quotes.controller.ts
TypeScript compiles: cd backend && npx tsc --noEmit
  </verify>
  <done>GET /quotes?page=1&limit=10&status=pending returns paginated results</done>
</task>

<task type="auto">
  <name>Task 3: Add stats endpoint for dashboard cards</name>
  <files>backend/src/modules/quotes/quotes.controller.ts,backend/src/modules/quotes/quotes.service.ts</files>
  <read_first>
- backend/src/modules/users/services.ts (existing stats pattern)
  </read_first>
  <action>
Add GET /quotes/stats endpoint:
- Query params: organisationId

Add getStats method to quotes.service.ts:
- Count by status: pending, quoted, accepted, rejected
- Total quotes
- Recent quotes (last 7 days)
- Return { total, pending, quoted, accepted, rejected, recent }
  </action>
  <verify>
grep -n "quotes/stats" backend/src/modules/quotes/quotes.controller.ts
grep -n "getStats\|stats" backend/src/modules/quotes/quotes.service.ts
  </verify>
  <done>GET /quotes/stats returns status breakdown counts</done>
</task>

<task type="auto">
  <name>Task 4: Add email triggers for status changes</name>
  <files>backend/src/modules/quotes/quotes.service.ts</files>
  <read_first>
- backend/src/modules/quotes/quotes.service.ts (update method)
- backend/src/modules/notifications/notifications.service.ts (existing pattern)
  </read_first>
  <action>
In quotes.service.ts update method, after status change:
- If status changed to 'quoted': trigger email to customer
- If status changed to 'accepted' or 'rejected': trigger email to customer
- Use notificationsService or separate emailService

Email content (per D-11):
- Status 'quoted': "Your quote is ready - $XX"
- Status 'accepted': "Your quote has been accepted"
- Status 'rejected': "Your quote was not accepted"
  </action>
  <verify>
grep -n "quoted.*email\|accepted.*email\|rejected.*email" backend/src/modules/quotes/quotes.service.ts
  </verify>
  <done>Status change triggers email notification to customer</done>
</task>

</tasks>

<verification>
Backend validation:
- cd backend && npx tsc --noEmit (TypeScript compiles)
- DELETE returns correct status for admin vs non-admin
- Pagination params work correctly
- Stats returns correct counts
</verification>

<success_criteria>
- [ ] DELETE /quotes/:id works (admin only)
- [ ] GET /quotes supports pagination, filtering, sorting
- [ ] GET /quotes/stats returns status breakdown
- [ ] Status changes trigger email to customer
</success_criteria>

<output>
After completion, create `.planning/phases/17-quote-management-we-need-same-layout-as-user-management-lets/17-01-SUMMARY.md`
</output>