---
phase: 17-quote-management
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - admin/app/quotes/page.tsx
  - admin/components/quote-stats-cards.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Quote page loads with table, stats cards, filters"
    - "Table shows quotes with sorting, search, filter, pagination"
    - "Edit quote status/price works"
    - "Delete quote works (admin only)"
  artifacts:
    - path: "admin/app/quotes/page.tsx"
      provides: "Quote management page with full CRUD"
      min_lines: 100
    - path: "admin/components/quote-stats-cards.tsx"
      provides: "Stats cards showing status breakdown"
      min_lines: 30
  key_links:
    - from: "admin/app/quotes/page.tsx"
      to: "/api/quotes"
      via: "api.get, api.patch, api.delete"
      pattern: "api\\.get.*quotes|api\\.patch.*quotes"
---

<objective>
Frontend Quote Management Page

Create quote management page following Phase 16 user management layout: table with sorting/search/filter/pagination, stats cards, CRUD operations, role-based permissions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-quote-management-we-need-same-layout-as-user-management-lets/17-CONTEXT.md
@.planning/phases/17-quote-management-we-need-same-layout-as-user-management-lets/17-RESEARCH.md
@admin/app/users/page.tsx (reference pattern - same layout to replicate)
@backend/src/database/schema/quotes.ts (Quote type)

**From CONTEXT.md:**
- D-02: Columns — email, origin, destination, status, dates
- D-03: Edit status/price (staff+admin), Delete (admin only)
- D-04: Stats cards with status breakdown
- D-06: Full filters — status, date range, email, origin/destination
- D-07: WebSocket push via EventsGateway

**From RESEARCH.md:**
- Backend already extended in Plan 01
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create quote management page</name>
  <files>admin/app/quotes/page.tsx</files>
  <read_first>
- admin/app/users/page.tsx (full reference - layout pattern to replicate)
- backend/src/database/schema/quotes.ts (Quote type definition)
- admin/lib/api.ts (API client pattern)
  </read_first>
  <action>
Create admin/app/quotes/page.tsx following users/page.tsx pattern:

Layout:
- Header with "Quote Management" title
- Stats cards row (QuoteStatsCards component)
- Filters row: search input, status filter, date range, origin/destination, pagination limit
- Table with columns: ID, Email, Origin, Destination, Status, Price, Created, Actions
- Pagination row

State:
- quotes: Quote[]
- total, page, limit, totalPages
- search, statusFilter, dateFrom, dateTo, originFilter, destinationFilter
- sortBy, sortOrder
- loading

API calls (using extended backend from Plan 01):
- GET /quotes?page=X&limit=Y&status=Z&search=Q&sortBy=A&sortOrder=B
- PATCH /quotes/:id { status, price } — for edit
- DELETE /quotes/:id — for delete

Role permissions (per D-03):
- canEdit: user.role === 'admin' || user.role === 'staff'
- canDelete: user.role === 'admin' only
- canView: user.role !== 'customer' (customers see own only - handled by backend)
  </action>
  <verify>
Test TypeScript: cd admin && npx tsc --noEmit
Page renders: grep -n "Quote Management" admin/app/quotes/page.tsx
  </verify>
  <done>Quote page at /quotes shows table with quotes data</done>
</task>

<task type="auto">
  <name>Task 2: Create quote stats cards component</name>
  <files>admin/components/quote-stats-cards.tsx</files>
  <read_first>
- admin/components/user-stats-cards.tsx (pattern to replicate)
  </read_first>
  <action>
Create admin/components/quote-stats-cards.tsx:

Props:
- total: number
- pending: number
- quoted: number
- accepted: number
- rejected: number
- recent?: number (last 7 days)

Layout (follow user-stats-cards pattern):
- 4-5 stat cards in a row
- Each card: icon, label, count
- Color coding: pending=yellow, quoted=blue, accepted=green, rejected=red

Card types:
- Total Quotes
- Pending (awaiting response)
- Quoted (price provided)
- Accepted
- Rejected
  </action>
  <verify>
grep -n "QuoteStatsCards" admin/app/quotes/page.tsx
grep -n "pending.*quoted.*accepted" admin/components/quote-stats-cards.tsx
  </verify>
  <done>Stats cards display status breakdown counts</done>
</task>

<task type="auto">
  <name>Task 3: Add WebSocket real-time updates</name>
  <files>admin/app/quotes/page.tsx,admin/lib/websocket.ts</files>
  <read_first>
- admin/lib/api.ts (existing API client)
- backend/src/modules/events/events.gateway.ts (WebSocket patterns)
- admin/app/dashboard/page.tsx (if WebSocket connection exists)
  </read_first>
  <action>
Add WebSocket connection to quotes page:

1. Connect to EventsGateway on page load
2. Join organisation room: client.emit('join-organisation', organisationId)
3. Listen for 'new-quote' event
4. On new-quote: show toast notification + re-fetch quotes

Events per D-07:
- Receive: 'new-quote' event from EventsGateway
- Action: Show notification toast, add to table or refetch
  </action>
  <verify>
grep -n "new-quote\|WebSocket\|socket" admin/app/quotes/page.tsx
  </verify>
  <done>New quote triggers real-time notification in dashboard</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted input (quote data, status) crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tamper | Quote status update | mitigate | Validate status enum on PATCH /quotes/:id server-side |
| T-17-02 | Tamper | Quote price update | mitigate | Validate price is positive number, max 2 decimals |
| T-17-03 | InfoDiscl | Quote email exposure | mitigate | Only expose email to staff/admin, not customer role |
| T-17-04 | Repudiation | Delete quote | mitigate | Log delete action with user ID |
</threat_model>

<verification>
Frontend validation:
- cd admin && npx tsc --noEmit (TypeScript compiles)
- Page loads without errors
- Table renders with mock data
- Stats cards display correctly
- Role permissions block unauthorized actions
</verification>

<success_criteria>
- [ ] Quote page at /quotes loads
- [ ] Table shows quotes with columns per D-02
- [ ] Stats cards show status breakdown per D-04
- [ ] Filters work per D-06
- [ ] Edit status/price works (staff+admin)
- [ ] Delete works (admin only)
- [ ] WebSocket updates work per D-07
</success_criteria>

<output>
After completion, create `.planning/phases/17-quote-management-we-need-same-layout-as-user-management-lets/17-02-SUMMARY.md`
</output>