---
phase: 01-admin-auth-connect
plan: "04"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - admin/app/(protected)/dashboard/page.tsx
  - admin/app/(protected)/shipments/page.tsx
  - admin/app/(protected)/quotes/page.tsx
  - admin/app/(protected)/notifications/page.tsx
  - admin/components/ui/table.tsx
autonomous: false
requirements:
  - ADMIN-05

must_haves:
  truths:
    - "Dashboard displays user name and fetches notifications"
    - "Shipments page shows data from /api/shipments"
    - "Quotes page shows data from /api/quotes"
    - "Notifications page shows data from /api/notifications"
  artifacts:
    - path: admin/app/(protected)/dashboard/page.tsx
      provides: Dashboard with user info and stats
    - path: admin/app/(protected)/shipments/page.tsx
      provides: Shipments list
    - path: admin/app/(protected)/quotes/page.tsx
      provides: Quotes list
    - path: admin/app/(protected)/notifications/page.tsx
      provides: Notifications list
  key_links:
    - from: dashboard/page.tsx
      to: /api/notifications
      via: api.get()
      pattern: "api\\.get.*notifications"
    - from: shipments/page.tsx
      to: /api/shipments
      via: api.get()
      pattern: "api\\.get.*shipments"
    - from: quotes/page.tsx
      to: /api/quotes
      via: api.get()
      pattern: "api\\.get.*quotes"
---

<objective>
Wire dashboard, shipments, quotes, and notifications pages to backend APIs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
Backend endpoints:
- GET /api/notifications - returns notifications array
- GET /api/shipments - returns shipments array
- GET /api/quotes - returns quotes array

All require JWT auth via httpOnly cookie (handled by api client).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard page</name>
  <files>admin/app/(protected)/dashboard/page.tsx</files>
  <action>
Create admin/app/(protected)/dashboard/page.tsx:

1. This is a Client Component ('use client')
2. Import useAuth from hooks/use-auth
3. Fetch notifications from /api/notifications on mount using useEffect
4. Display:
   - Welcome message with user.name from useAuth()
   - Stats cards: Pending Shipments count, Active Quotes count, Unread Notifications count
   - Recent Notifications list (last 5)
5. Use shadcn Card components (may need to add: npx shadcn@latest add card)
6. Show loading state while fetching
7. Handle empty states gracefully

Backend types (from notifications service):
```typescript
interface Notification {
  id: string;
  organisationId: string;
  userId: string;
  titleKey: string;
  data: Record<string, any>;
  isRead: boolean;
  createdAt: Date;
  expiresAt: Date;
}
```
</action>
  <verify>
    <automated>cd admin && npm run typecheck</automated>
  </verify>
  <done>Dashboard displays user name and shows notifications from backend</done>
</task>

<task type="auto">
  <name>Task 2: Create shipments page</name>
  <files>admin/app/(protected)/shipments/page.tsx</files>
  <action>
Create admin/app/(protected)/shipments/page.tsx:

1. Client Component that fetches from /api/shipments
2. Add table component: npx shadcn@latest add table -y
3. Display shipments as table with columns:
   - Tracking Number
   - Status (use Badge component)
   - Origin
   - Destination
   - Created Date
4. Loading skeleton during fetch
5. Empty state: "No shipments yet"

Backend shipment type (from shipments service):
```typescript
interface Shipment {
  id: string;
  trackingNumber: string;
  status: 'pending' | 'in_transit' | 'delivered' | 'cancelled';
  origin: string;
  destination: string;
  createdAt: Date;
  // ... other fields
}
```
</action>
  <verify>
    <automated>cd admin && npm run typecheck</automated>
  </verify>
  <done>Shipments page displays list from backend API</done>
</task>

<task type="auto">
  <name>Task 3: Create quotes page</name>
  <files>admin/app/(protected)/quotes/page.tsx</files>
  <action>
Create admin/app/(protected)/quotes/page.tsx:

1. Client Component that fetches from /api/quotes
2. Reuse table component from shipments page
3. Display quotes as table with columns:
   - Quote Number
   - Customer Name
   - Status
   - Amount
   - Created Date
4. Loading skeleton during fetch
5. Empty state: "No quotes yet"

Backend quote type (from quotes service):
```typescript
interface Quote {
  id: string;
  quoteNumber: string;
  customerName: string;
  status: 'draft' | 'sent' | 'accepted' | 'rejected';
  totalAmount: number;
  createdAt: Date;
}
```
</action>
  <verify>
    <automated>cd admin && npm run typecheck</automated>
  </verify>
  <done>Quotes page displays list from backend API</done>
</task>

<task type="auto">
  <name>Task 4: Create notifications page</name>
  <files>admin/app/(protected)/notifications/page.tsx</files>
  <action>
Create admin/app/(protected)/notifications/page.tsx:

1. Client Component that fetches from /api/notifications
2. Display notifications as list with:
   - Title (from titleKey)
   - Created date
   - Read/Unread status (show icon or badge)
   - Mark as read button (PATCH /api/notifications/:id)
3. Add badge component: npx shadcn@latest add badge -y
4. Add ability to mark individual notifications as read
5. Add "Mark all as read" button (PATCH /api/notifications with query ?markAll=true)
6. Loading and empty states
</action>
  <verify>
    <automated>cd admin && npm run typecheck</automated>
  </verify>
  <done>Notifications page displays list and allows marking as read</done>
</task>

</tasks>

<verification>
1. cd admin && npm run typecheck - no errors
2. Login at /login redirects to /dashboard
3. Dashboard shows user name from /api/auth/me
4. Dashboard shows notifications from /api/notifications
5. Navigate to /shipments shows shipments list
6. Navigate to /quotes shows quotes list
7. Navigate to /notifications shows notifications with mark-as-read
</verification>

<success_criteria>
- Dashboard displays user name and fetches notifications
- Shipments page shows data from /api/shipments in table
- Quotes page shows data from /api/quotes in table
- Notifications page shows data from /api/notifications with mark-as-read
- All pages show loading states and empty states
- npm run typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-admin-auth-connect/01-04-SUMMARY.md`
</output>
