---
phase: 01-admin-auth-connect
plan: "02"
type: execute
wave: 2
depends_on: []
files_modified:
  - admin/app/login/page.tsx
  - admin/components/ui/input.tsx
  - admin/components/ui/label.tsx
  - admin/components/ui/card.tsx
autonomous: true
requirements:
  - ADMIN-03

must_haves:
  truths:
    - "Login page renders at /login with email/password form"
    - "Successful login redirects to /dashboard"
    - "Failed login shows error message"
  artifacts:
    - path: admin/app/login/page.tsx
      provides: Login page with form
    - path: admin/components/ui/input.tsx
      provides: Form input component
    - path: admin/components/ui/label.tsx
      provides: Form label component
    - path: admin/components/ui/card.tsx
      provides: Card container component
  key_links:
    - from: login/page.tsx
      to: use-auth.ts
      via: useAuth().login()
      pattern: "useAuth.*login"
    - from: login/page.tsx
      to: /dashboard
      via: useRouter().push()
      pattern: "router.*dashboard"
---

<objective>
Build login UI with email/password form using shadcn components.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
Project uses shadcn/ui v4 with radix-nova style. Components installed via:
```bash
npx shadcn@latest add [component]
```

Current components available: button, theme-provider
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add form input components via shadcn</name>
  <files>
    - admin/components/ui/input.tsx
    - admin/components/ui/label.tsx
    - admin/components/ui/card.tsx
    - admin/components/ui/card-header.tsx
    - admin/components/ui/card-content.tsx
    - admin/components/ui/card-footer.tsx
  </files>
  <action>
Run shadcn CLI to add required components:
```bash
cd admin && npx shadcn@latest add input label card -y
```

This creates:
- input.tsx - Text input with shadcn styling
- label.tsx - Accessible form label
- card.tsx, card-header.tsx, card-content.tsx, card-footer.tsx - Card container
</action>
  <verify>
    <automated>Test-Path admin/components/ui/input.tsx -And (Test-Path admin/components/ui/label.tsx) -And (Test-Path admin/components/ui/card.tsx)</automated>
  </verify>
  <done>Form input components available in admin/components/ui/</done>
</task>

<task type="auto">
  <name>Task 2: Create login page</name>
  <files>admin/app/login/page.tsx</files>
  <action>
Create admin/app/login/page.tsx:
1. This is a React Server Component (default in Next.js 16 app router)
2. Create a client component "LoginForm" inside for form handling
3. Use shadcn Card to center the form on page
4. Form fields: email (type="email"), password (type="password")
5. On submit: call useAuth().login(email, password)
6. On success: useRouter().push('/dashboard')
7. On error: display error message below form
8. Add "Continue with Google" button linking to /api/auth/google
9. Style: centered vertically and horizontally, max-width 400px
10. Dark mode support via theme-provider already in layout

Use shadcn classes:
- Card: border bg-card text-card-foreground shadow-sm
- Input: flex h-9 w-full rounded-md border bg-transparent px-3 py-1
- Button: h-9 px-4 py-2 (default variant)

Keep it simple - no fancy animations, just functional form.
</action>
  <verify>
    <automated>cd admin && npm run typecheck</automated>
  </verify>
  <done>Login page at /login renders form, handles submit, redirects on success</done>
</task>

</tasks>

<verification>
1. cd admin && npm run typecheck - no errors
2. cd admin && npm run dev - page available at localhost:3000/login
3. Form displays email and password fields
4. Submit button calls login handler
</verification>

<success_criteria>
- /login page renders with centered card containing form
- Email and password fields functional
- Submit calls useAuth().login()
- Success redirects to /dashboard
- Error displays message
- Google OAuth button present
</success_criteria>

<output>
After completion, create `.planning/phases/01-admin-auth-connect/01-02-SUMMARY.md`
</output>
